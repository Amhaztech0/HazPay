# STATUS FEATURE - VISUAL ARCHITECTURE

## App Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      HomeScreen                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │        StatusList (Horizontal Scrollable)           │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  [+ My  │  [User1 │  [User2 │  [User3 │          │   │
│  │ Status] │ Status] │ Status] │ Status] │ ...      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         ChatList (Vertical Scrollable)              │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  Test User 1                                        │   │
│  │  "Amazing 😄😄😄"                                   │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  Test User 2                                        │   │
│  │  "Last message..."                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow

### Creating a Status

```
User taps "+ My Status"
          │
          ▼
    CreateStatusScreen
     - Choose type
     - Enter content
     - Pick colors/media
          │
          ▼
   StatusService.create*Status()
     - Upload to storage (if media)
     - Insert into status_updates table
          │
          ▼
  Supabase API
     │
     ├─► storage.objects (if photo/video)
     │
     └─► status_updates table
          │
          ▼
    Status saved with:
     - user_id
     - content
     - media_url (optional)
     - media_type (text/image/video)
     - created_at
     - expires_at (now + 24h)
          │
          ▼
    HomeScreen._loadData() refreshes
          │
          ▼
    StatusList updates with new status
          │
          ▼
    User sees status in bar immediately
```

### Viewing a Status

```
User taps status in bar
          │
          ▼
   StatusViewerScreen
     - Shows full screen
     - Starts 5-second timer
     - Shows progress bars
          │
          ▼
   markStatusAsViewed() called
     - Inserts into status_views table
     - Links current user as viewer
          │
          ▼
   Auto-advance after 5s
     - Load next status
     - Reset timer
     - Repeat
          │
          ▼
   User swipes/taps to next
     - Manual navigation
     - Bypasses timer
          │
          ▼
   All statuses consumed
     - Close viewer
     - Return to home
```

---

## Database Schema

```
┌─────────────────────────────────┐
│      status_updates             │
├─────────────────────────────────┤
│ id (UUID) PRIMARY KEY           │
│ user_id (UUID) FK               │──┐
│ content (TEXT)                  │  │
│ media_url (TEXT)                │  │
│ media_type (TEXT)               │  │
│ background_color (TEXT)         │  │
│ created_at (TIMESTAMP)          │  │
│ expires_at (TIMESTAMP)          │  │
└─────────────────────────────────┘  │
                                     │
                    ┌────────────────┘
                    │
┌───────────────────▼────────────────┐
│        auth.users                  │
├────────────────────────────────────┤
│ id (UUID) PRIMARY KEY              │
│ email (TEXT)                       │
│ email_confirmed_at (TIMESTAMP)     │
└────────────────────────────────────┘
                    ▲
                    │
        ┌───────────┴────────────┐
        │                        │
┌───────┴──────────────────┐    │
│    status_views          │    │
├──────────────────────────┤    │
│ id (UUID) PRIMARY KEY    │    │
│ status_id (UUID) FK  ────┼────┤
│ viewer_id (UUID) FK  ────┼────┤
│ viewed_at (TIMESTAMP)    │    │
└──────────────────────────┘    │
```

---

## File Structure

```
ZinChat/
├── lib/
│   ├── screens/
│   │   ├── home/
│   │   │   └── home_screen.dart          ← Loads statuses, shows StatusList
│   │   ├── status/
│   │   │   ├── create_status_screen.dart ← Post new status
│   │   │   └── status_viewer_screen.dart ← View in full screen
│   │   └── chat/
│   │       ├── chat_screen.dart
│   │       └── new_chat_screen.dart
│   ├── services/
│   │   ├── status_service.dart           ← API for all status operations
│   │   ├── storage_service.dart
│   │   ├── chat_service.dart
│   │   └── auth_service.dart
│   ├── models/
│   │   ├── status_model.dart             ← StatusUpdate & UserStatusGroup
│   │   ├── chat_model.dart
│   │   └── user_model.dart
│   └── widgets/
│       ├── status_list.dart              ← Status bar component
│       └── chat_tile.dart
├── supabase/
│   └── migrations/
│       └── 20250108_create_status_tables.sql  ← DATABASE SETUP (RUN THIS!)
└── STATUS_TABLES.sql                    ← Copy of migration for easy access
```

---

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                    HomeScreen                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  _loadData()                                         │  │
│  │  - Calls StatusService.getAllStatuses()             │  │
│  │  - Calls ChatService.getUserChats()                 │  │
│  │  - Updates _statusGroups & _chats                   │  │
│  └──────────────────────────────────────────────────────┘  │
│              │                          │                   │
│              ▼                          ▼                   │
│  ┌──────────────────────────┐ ┌─────────────────────────┐  │
│  │   StatusList Widget      │ │  ChatList Widget        │  │
│  │ - Shows status bar       │ │ - Shows chats           │  │
│  │ - Handles tap events     │ │ - Navigates to chat     │  │
│  │ - Displays avatars       │ │                         │  │
│  └──────────┬───────────────┘ └─────────────────────────┘  │
│             │                                               │
│             ├──► tap "+ My Status"                         │
│             │      │                                        │
│             │      ▼                                        │
│             │  CreateStatusScreen                          │
│             │  - Text status with colors                   │
│             │  - Photo picker                              │
│             │  - Video picker                              │
│             │  - Calls StatusService.create*()             │
│             │                                              │
│             └──► tap user status                           │
│                  │                                          │
│                  ▼                                          │
│            StatusViewerScreen                              │
│            - Full screen immersive                         │
│            - Progress bars                                │
│            - Auto-advance timer                           │
│            - Tap navigation                               │
│            - Calls markStatusAsViewed()                   │
└─────────────────────────────────────────────────────────────┘
           │                           │
           ▼                           ▼
   ┌──────────────────┐       ┌──────────────────┐
   │ StatusService    │       │  ChatService     │
   │ - getAllStatuses │       │ - getUserChats   │
   │ - createText...  │       │ - getMessages    │
   │ - createMedia... │       │ - sendMessage    │
   │ - markViewed     │       │                  │
   └────────┬─────────┘       └────────┬─────────┘
            │                         │
            └─────────────┬───────────┘
                          ▼
                  ┌──────────────────┐
                  │ Supabase API     │
                  ├──────────────────┤
                  │ Tables:          │
                  │ - status_updates │
                  │ - status_views   │
                  │ - messages       │
                  │ - chats          │
                  │                  │
                  │ Storage:         │
                  │ - status-media   │
                  │ - chat-media     │
                  └──────────────────┘
```

---

## State Management Flow

```
HomeScreen State:
  _statusGroups: List<UserStatusGroup>
  _chats: List<ChatModel>
  _isLoading: bool

  _loadData() ─────┐
                  ▼
          Supabase Query
                  │
     ┌────────────┴────────────┐
     │                         │
     ▼                         ▼
StatusGroups            ChatList
     │                    │
     └────────┬───────────┘
              ▼
          setState()
              │
     ┌────────┴────────┐
     │                 │
     ▼                 ▼
StatusList          ChatList
   Build             Build
     │                 │
     └────────┬────────┘
              ▼
          UI Updates
```

---

## Security & RLS Policies

```
┌────────────────────────────────────────────────────────┐
│           Row-Level Security (RLS)                     │
├────────────────────────────────────────────────────────┤
│                                                        │
│ status_updates:                                        │
│ - SELECT: Anyone can view active (< 24h) statuses     │
│ - INSERT: Only creator (auth.uid = user_id)           │
│ - DELETE: Only creator                                │
│                                                        │
│ status_views:                                          │
│ - SELECT: Anyone (read receipts are public)           │
│ - INSERT: Only authenticated users                    │
│ - Unique(status_id, viewer_id): One view per person   │
│                                                        │
│ storage.status-media:                                  │
│ - INSERT: Authenticated users only                    │
│ - SELECT: Public (anyone can download)                │
│ - DELETE: Owner only (determined by folder path)      │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## Timeline: Status Lifecycle

```
Timeline:
┌──────────────────────────────────────────────────────────┐
│ T=0                                                      │
│ User creates status                                      │
│ created_at: NOW()                                        │
│ expires_at: NOW() + 24 hours                             │
│                                                          │
│ Status shows in bar immediately                          │
│ ✅ Visible to all users                                  │
└──────────────────────────────────────────────────────────┘
         │
         │ ... 23 hours pass ...
         │
┌────────▼──────────────────────────────────────────────────┐
│ T=23h                                                     │
│ Status still active                                       │
│ expires_at: 1 hour from now                              │
│                                                          │
│ ✅ Still visible in bar                                   │
│ ✅ Can still be viewed                                    │
└───────────────────────────────────────────────────────────┘
         │
         │ ... 1 more hour passes ...
         │
┌────────▼──────────────────────────────────────────────────┐
│ T=24h (NOW() ≥ expires_at)                                │
│ Query: WHERE expires_at > NOW()                           │
│ Status no longer matches condition                        │
│                                                          │
│ ❌ Disappears from status bar                             │
│ ❌ Not queryable anymore                                  │
│ ⏱️ Optional: cleanupExpiredStatuses() deletes record      │
└───────────────────────────────────────────────────────────┘
```

---

## Ready to Deploy!

All components are implemented and working. Just need to:

1. Run the SQL migration
2. Refresh the app
3. Start posting statuses! 🎉
